{
  "rules": {
    "presence": {
      "$roomCode": {
        "$sessionId": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid === $sessionId"
        }
      }
    }
  }
}

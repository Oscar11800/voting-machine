{
  "rules": {
    "presence": {
      "$roomCode": {
        ".read": "auth != null",
        "$sessionId": {
          ".write": "auth != null && auth.uid === $sessionId"
        }
      }
    }
  }
}
